# Environment Configuration
# Copy this file to .env and modify as needed
#
# Read by `docker compose` (docker-compose.yml) to publish the Postgres
# containers on the host. Override these per workspace to run multiple
# workspaces' databases in parallel without port collisions — see
# scripts/setup_parallel_workspace.sh.

DATABASE_PORT=5432
DATABASE_TEST_PORT=5433
