# Vite dev server port — override per workspace to avoid collisions
WEB_PORT=5173

# API Configuration
# Port must match the api's PORT / root DATABASE_PORT setup
VITE_API_URL=http://localhost:3000

# Auth Configuration
# Set to true to use fake auth provider (dev only)
VITE_FAKE_AUTH=true
