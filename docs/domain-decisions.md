# Domain Decisions

> **⚠ Replace the example row when you fork the template.** The template ships
> no domain decisions of its own — technical decisions live in [adr/](./adr/).

The single home for **domain / product design decisions** and their rationale
(technical/architectural decisions live in [docs/adr/](./adr/) instead).

Append-only — add a new numbered row when a domain decision is made; mark
superseded rows rather than deleting them. The entities and rules in
[CONTEXT.md](../CONTEXT.md) always reflect the _current_ outcome of this table.

| #   | Decision                              | Rationale                        |
| --- | ------------------------------------- | -------------------------------- |
| 1   | <the decision, stated as the outcome> | <why this over the alternatives> |
